{
  "name": "astrojs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:build": "npm run build && npm run preview",
    "serve:dist": "npx serve dist",
    "build:relative": "npm run build && node scripts/make-relative.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.13.9"
  }
}